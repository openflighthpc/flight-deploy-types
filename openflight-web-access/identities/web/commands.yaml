apply:
  - name: Apply Web Identity 
    command: "ansible-playbook -i $INVFILE --limit $NODE --extra-vars=\"cluster_name=web default_username=$DEFAULT_USERNAME default_user_password=$DEFAULT_PASSWORD access_host=$ACCESS_HOST\" $RUN_ENV/main.yml"
