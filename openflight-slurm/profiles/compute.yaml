name: compute
description: Set up as a compute node with Flight Runway and job-running tools
group_name: nodes
command: "ansible-playbook -i $INVFILE --limit $NODE --extra-vars=\"cluster_name=$CLUSTERNAME compute_ip_range=$IPRANGE\" $DEPLOYDIR/openflight-ansible-playbook/main.yml"
